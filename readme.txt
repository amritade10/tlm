We have a tlm_initiator_socket in the initiator, a tlm_target_socket to the memory,
and binded the two sockets together at the top level.

We have a dummy implementations of the remaining forward and backward
interface methods. 

